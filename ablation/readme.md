For running the **evaluate_local.py**, for evaluating GRU Decoder only

```bash
python evaluate_local.py --model_path trained_models/baseline_rnn --eval_type val
```
For running the **evaluate_local.py**, for evaluating GRU Decoder only, outputs a .txt file for the results
```bash
python evaluate_local.py --model_path trained_models/baseline_rnn --eval_type val > evaluation_results.txt
```